# AI-Powered Enterprise Data Analytics & Reporting Platform
## 改進追蹤文檔 v3(原始碼驗證版 / Code-Verified）

> 本版與前兩版最大的差異:前兩版是根據README文字分析,這一版是實際載入原始碼、追蹤執行路徑後的驗證結果。部分README聲稱已解決的項目,在程式碼層級被發現與敘述有落差,列為本版最高優先。

---

## 一、已用原始碼驗證為真實實作的項目

以下機制經過直接閱讀程式碼確認,不是紙上談兵:

| 機制 | 驗證位置 | 備註 |
|---|---|---|
| Circuit Breaker + 指數退避重試 | `ai/resilience.py` | 真正的CLOSED/OPEN/HALF_OPEN狀態機,實作正確 |
| Token/成本治理(RPM限流、每日預算、重放偵測) | `security/token_governance.py` | Sliding-window實作正確,但**僅記憶體內**(見下方3.3) |
| CI/CD 供應鏈掃描 | `.github/workflows/ci.yml` | 真的執行`ruff`與`pip-audit`(但見下方3.1的保留意見) |
| Dependabot自動更新 | `.github/dependabot.yml` | 真實配置,涵蓋pip/npm/github-actions三個生態系 |
| RLS/CLS的AST改寫技術路線 | `query_engine/rls_enforcer.py` | 使用`sqlglot`做語法樹層級的謂詞注入,手法正確(但見下方2.1的範圍問題) |

---

## 二、重大落差(Critical — 敘述與程式碼實際行為不一致)

### 2.1 「Mandatory RLS」未實際套用到6個真實資料集

**問題**:`rls_enforcer.py`裡的範圍設定是精確字串比對:

```python
TENANT_SCOPED_TABLES = {"orders", "customers"}
REGION_SCOPED_TABLES = {"customers", "regions"}
```

但實際ingest的表名是`olist_orders`、`olist_customers`等(確認於`ingestion/datasets/olist_ecommerce.py`),與上述集合完全不匹配。且進一步確認這6個真實資料集的schema裡**根本沒有`tenant_id`欄位**。

**影響範圍**:
- `rls_enforcer`確實被`ai/agent/analyst_agent.py`在真實查詢流程中呼叫,不是只用在demo
- 但因表名不匹配,對6個真實資料集的任何查詢,RLS謂詞注入永遠不會觸發,SQL會原封不動通過
- 「Mandatory RLS」目前只在「SQL Security Inspector」沙盒(使用它自己虛構的`orders`/`customers`/`regions`範例schema)中被真正展示,並未作用於AI Analyst對真實資料的查詢路徑

**為什麼重要**:這是整個專案「LLM是不受信任的規劃者」核心敘事裡最關鍵的一道防線,如果面試官深挖「這在olist資料集上實際生效嗎」,目前的答案會跟README不一致。

**建議修法**:
- 短期:在README/Known Limitations誠實區分「機制已在SQL Inspector沙盒驗證」vs.「6個公開資料集因無原生tenant_id欄位,暫未套用」
- 長期:讓`TENANT_SCOPED_TABLES`從dataset catalog動態產生;針對這批單租戶性質的公開資料,改用region-based或department-based範圍限定取代tenant_id-based

### 2.2 SSO/OIDC callback 完全沒有驗證 token 簽章(安全漏洞)

**問題**:`auth.py`的`oidc_callback`函式註解寫著「In production, this verifies signature via IdP JWKS endpoint」,但目前程式碼**直接base64解碼JWT payload,沒有任何簽章驗證**。你自己的`tests/security/test_sso_oidc.py`裡,構造的mock token簽章欄位就是寫死的`"fake_signature"`字串,而這個假簽章的token被系統正常接受並核發JWT——測試本身就證明了這個漏洞。

**影響範圍**:只要`ENABLE_OIDC_SSO=true`,任何人都能偽造payload(讓`groups`或`email`包含觸發條件的字串),直接取得`ORG_ADMIN`角色或指定任意`tenant_id`,完全繞過身份驗證。

**為什麼重要**:一個主打deterministic security boundary的專案,新加的SSO功能卻省略最基本的密碼學驗證,這個反差本身就是資安面試會直接問到的點。

**建議修法**:改用已在用的`python-jose[cryptography]`,透過IdP的JWKS endpoint取得公鑰,執行`jwt.decode(token, key, algorithms=["RS256"], audience=..., issuer=...)`做真正的簽章與claims驗證,取代手動base64解碼。

---

## 三、中優先 — 實作細節瑕疵

### 3.1 CI掃描步驟用`|| true`,實際上不會擋下任何問題

`ci.yml`裡:
```yaml
ruff check backend/ tests/ --exit-non-zero-on-fix || true
pip-audit -r backend/requirements.txt --desc on --ignore-vuln CVE-2024-5171 || true
```
`|| true`會讓這兩個步驟無論結果如何都回報成功,等於「有跑掃描」但掃描結果不會讓build失敗。建議至少讓`pip-audit`在發現CRITICAL/HIGH漏洞時真正讓CI失敗。

### 3.2 Dependabot設定引用不存在的審核團隊

`dependabot.yml`裡`reviewers: security-team`,對個人專案而言這個GitHub team並不存在,PR可能無法正確指派審核人。屬於企業範本複製後未依實際情境調整的細節。

### 3.3 Token治理僅記憶體內,多實例部署會失效

`token_governance.py`的docstring自己註明「in production, backed by Redis」——目前是單一process記憶體內計數,如果之後用多個worker/多實例部署,每個實例會各自計數,實際限流會失效。這是自我揭露的限制,不是隱藏問題,但值得明確寫進Known Limitations表格。

---

## 四、延續自前一版、仍未解決的項目

### 4.1 靜態加密(Encryption at Rest)仍未實作
確認`requirements.txt`裡沒有任何加密相關套件(`cryptography`/`fernet`等僅用於JWT簽署,非資料靜態加密)。與README的Known Limitations揭露一致,屬於已知且誠實揭露的限制。

### 4.2 缺乏可直接訪問的 Live Demo
仍只能透過clone+本機/Docker啟動體驗,沒有部署好的公開連結。

### 4.3 併發/效能benchmark數據仍未發布
`concurrency_benchmark.py`存在,但README沒有放上實際的P50/P95/P99數據。

---

## 五、優先順序建議

| 順序 | 項目 | 理由 |
|---|---|---|
| 1 | 2.2 SSO簽章驗證 | 真實安全漏洞,且修法明確、成本不高(用既有的python-jose即可) |
| 2 | 2.1 RLS範圍不匹配 | 核心敘事的可信度問題,至少要先誠實揭露範圍,再視時間決定是否修復機制 |
| 3 | 3.1 CI的`\|\| true` | 修改成本極低,但明顯提升CI的實質把關能力 |
| 4 | 4.2 Live Demo / 4.3 Benchmark數據 | 投入報酬比最高的展示型改善 |
| 5 | 3.2、3.3、4.1 | 可以先誠實寫進Known Limitations表格,不急著在短期內完全實作 |

---

*文件版本:2026年9月(v3 — 基於原始碼實際追蹤驗證,取代僅依README分析的v1/v2)*
