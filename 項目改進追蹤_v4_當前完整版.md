# AI-Powered Enterprise Data Analytics & Reporting Platform
## 改進追蹤文檔 v4(當前完整版 / Consolidated Current Status）

> 本版整合歷次審查的全部項目,並依最新一次上傳的原始碼(本機工作副本,尚未push至GitHub main)重新驗證每一項的實際狀態。分為「已完全解決」、「部分解決」、「尚未解決」三類,並在最後給出優先順序建議。

---

## 一、已完全解決(Fully Resolved — 有程式碼/測試證據)

| # | 項目 | 驗證證據 |
|---|---|---|
| 1 | "Enterprise-grade" 用詞與規模不符 | README重新定位為「Architectural Reference Implementation」,並附Sizing Note說明治理層與儲存層解耦 |
| 2 | "100% Fact-Checked Claims" 缺乏方法論 | 改為regex數值抽取+容差比對(±0.5%),分類SUPPORTED/UNSUPPORTED/APPROXIMATED,附審計軌跡 |
| 3 | LLM API呼叫缺乏成本/濫用治理 | `security/token_governance.py`:真實的sliding-window RPM限流、每日token/成本預算、重放偵測(SHA256) |
| 4 | LLM API故障容錯機制未說明 | `ai/resilience.py`:真正的CircuitBreaker狀態機(CLOSED/OPEN/HALF_OPEN)+ 指數退避重試,實作正確 |
| 5 | 依賴/映像檔弱點掃描缺失(SCA) | `.github/workflows/ci.yml`真實執行`pip-audit`;`.github/dependabot.yml`真實配置(pip/npm/actions三生態系) |
| 6 | 語意層版本控管未描述 | Metric Registry納入版本號、責任人、審計歷程metadata |
| 7 | **SSO/OIDC callback 無簽章驗證(原CRITICAL級漏洞)** | 新增`security/oidc.py`,真正的RS256簽章驗證(演算法白名單、拒絕`alg=none`、過期/audience/issuer檢查)。測試新增`test_oidc_callback_rejects_fake_signature`,故意用篡改signature測試,斷言必須401並回報「signature verification failed」。**漏洞→修復→回歸測試的完整閉環** |
| 8 | **CI掃描步驟`\|\| true`,不會真正擋build** | `ruff check`與`pip-audit`已移除`\|\| true`,現在會讓CI真正失敗 |

---

## 二、部分解決(Partially Resolved)

### 2.1 RLS/RBAC範圍——Region/Department維度已解決,Tenant維度仍未解決

**已解決的部分**:`query_engine/rls_enforcer.py`重寫成動態的`tenant_column_map`/`region_column_map`/`department_column_map`,並:
- 新增5張企業合成資料表(`sales_orders`、`inventory_supply_chain`、`financial_metrics`、`employee_performance`、`marketing_campaigns`),這些表確實有多個region/department值,region/department層級的RBAC現在**可被實際demo驗證**
- 對6個真實公開資料集(`olist_orders`等),新增誠實的「PUBLIC_DATASET_GOVERNANCE」稽核項目,明確標注「無租戶邊界」,而不是假裝有隔離或悄悄不處理

**仍未解決的部分**:`tenant_column_map`目前仍只有`{"orders": "tenant_id", "customers": "tenant_id"}`這兩張示範表,而`seed_data.py`(已確認完全未變動)裡這兩張表**只有一個租戶`tenant-acme`**。也就是說,組織層級(tenant-to-tenant)的隔離,目前依然沒有第二個租戶可以用來證明「A租戶看不到B租戶資料」——這是「多租戶隔離」核心賣點裡唯一還沒真正解決的部分。

**建議**:在seed data裡加入至少第二個租戶(例如`tenant-globex`)並塞幾筆`orders`/`customers`資料,才能讓SQL Inspector的demo真正演示跨租戶隔離,而不只是概念上存在。

---

## 三、尚未解決(Not Yet Resolved)

### 高優先

**3.1 登入端點沒有brute-force防護**
`auth.py`的`login`函式沒有rate limit、失敗次數鎖定、或帳號封鎖機制。任何人可以對`admin@acme.com`無限次嘗試密碼。
建議:加入簡單的失敗次數計數(可比照`token_governance.py`已有的sliding-window手法),超過閾值鎖定帳號或延長重試間隔。

**3.2 靜態加密(Encryption at Rest)未實作**
確認`requirements.txt`無任何加密相關套件。DuckDB分析檔、PostgreSQL、備份檔案皆未加密。
建議:至少為PostgreSQL啟用at-rest encryption;若部署在雲端,善用物件儲存的伺服器端加密。

### 中優先

**3.3 Dependabot治理債務**
GitHub上目前有22個未合併的PR,大多應是Dependabot自動開出的依賴更新。有機制但沒人維護,比沒有機制更容易在審查時被扣分。
建議:清一輪,合併能合的、關掉不需要的,維持這個數字在個位數。

**3.4 Dependabot設定引用不存在的審核團隊**
`dependabot.yml`裡`reviewers: security-team`,個人專案沒有這個GitHub team,PR可能無法正確指派。

**3.5 Token治理僅記憶體內,多實例部署會失效**
`token_governance.py`自己的docstring承認「in production, backed by Redis」,目前單一process記憶體計數,多實例部署下限流會失效。屬於已自我揭露的限制,建議明確寫進Known Limitations表格。

### 加分項(非必要,但能大幅提升展示效果)

**3.6 缺乏可直接訪問的 Live Demo**
仍只能clone+本機/Docker啟動體驗,沒有部署好的公開連結。

**3.7 併發/效能benchmark數據未發布**
`concurrency_benchmark.py`已存在,但README沒有放上實際的P50/P95/P99數據。

**3.8 啟動方式重複**
`run.py` + `Makefile` + `start_all.bat` + `start_all.ps1` + `start_backend.bat` + `start_frontend.bat`——六種方式做同一件事,建議精簡為Docker Compose + Makefile兩種即可。

---

## 四、優先順序建議

| 順序 | 項目 | 理由 |
|---|---|---|
| 1 | 3.1 登入brute-force防護 | 真實的身份驗證基本防線缺口,且修法明確、成本不高 |
| 2 | 2.1 補第二個租戶到seed data | 「多租戶隔離」是整個專案最核心的賣點,目前連demo資料都不支援,補資料成本很低但影響敘事完整性很大 |
| 3 | 3.3 清理Dependabot的22個PR | 成本極低,直接影響外部審查者對「這個專案是否被積極維護」的第一印象 |
| 4 | 3.6 Live Demo / 3.7 Benchmark數據 | 投入報酬比最高的展示型改善,面試前應優先準備 |
| 5 | 3.2、3.4、3.5、3.8 | 可以先誠實寫進Known Limitations表格,不急著在短期內全部實作完成 |

---

## 五、整體結論

這一輪的修復品質相當高——SSO簽章驗證跟CI gating不是表面調整,是有測試覆蓋的真實修復,這種「找到自己專案裡的漏洞、修好、寫回歸測試」的迴圈,本身就是很強的面試素材。剩下的項目裡,只有**3.1(登入防護)**跟**2.1(補第二租戶)**還牽涉到核心安全承諾,其餘都是工程衛生或展示層面的加分項,不影響架構的根本可信度。

---

*文件版本:2026年9月(v4 — 整合歷次審查,取代v1/v2/v3)*
